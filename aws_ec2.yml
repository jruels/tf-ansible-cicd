plugin: aws_ec2
regions:
  - us-west-2
filters:
  instance-state-name: running
keyed_groups:
  - key: tags['role']
    prefix: tag_role
    separator: "_"
  - key: tags['Name']
    prefix: name
    separator: "_"
hostnames:
  - ip-address
compose:
  ansible_host: public_ip_address
